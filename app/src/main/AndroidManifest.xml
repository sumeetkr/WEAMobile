<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sv.cmu.edu.weamobile" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="sv.cmu.edu.weamobile.SettingsActivity"
            android:label="@string/app_name" >
        </activity>

        <service
            android:name="sv.cmu.edu.weamobile.service.BackgroundService"
            android:exported="false" >
        </service>

        <activity
            android:name="sv.cmu.edu.weamobile.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="sv.cmu.edu.weamobile.AlertActivity"
            android:label="@string/title_activity_alert" >
        </activity>
    </application>

</manifest>
